sudo: required

branches:
  only: 
    - master

services:
  - docker

before_install:
  - docker run --rm --privileged multiarch/qemu-user-static:register --reset
  - docker build -t test .

script:
  - docker run test /bin/sh -c "npm run test"
  - docker run test /bin/sh -c "npm run deploy"