sudo: required

language: node_js

cache:
  directories:
    - "node_modules"

node_js: 
  - "stable"
  
services:
  - docker

env:
  global:
    - IMAGE_NAME=armv7/armhf-ubuntu
    - USER_NAME=aj03794

before_script:
  # - printenv
  # - echo IMAGE_NAME USER_NAME
  # - docker pull armv7/armhf-ubuntu
  - docker build -t test .
  - docker images

before_install:

install:
  - npm install

script:
  - docker run test3 /bin/sh -c "npm run test"